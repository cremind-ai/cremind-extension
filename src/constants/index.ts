export const CONTENT_SCRIPT_PORT_NAME = "IPC";
export enum AIMode {
  OPENAI_API = "OPENAI_API",
  CHAT_GPT = "CHAT_GPT",
  BARD = "BARD",
}

export enum ConversationRoleEnum {
  USER = 0,
  ASSISTANT = 1,
}
